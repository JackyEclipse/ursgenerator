# URS Generator Backend Configuration
# Copy this file to .env and configure as needed

# =============================================================================
# LLM Configuration
# =============================================================================

# LLM Mode: "mock" (default) or "real"
# Mock mode returns realistic fake responses - no API key needed
LLM_MODE=mock

# LLM Provider: "openai" or "azure"
LLM_PROVIDER=openai

# Model to use (only for real mode)
LLM_MODEL=gpt-4o-mini

# OpenAI API Key (only needed for LLM_MODE=real)
OPENAI_API_KEY=sk-your-key-here

# Azure OpenAI (if using Azure)
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# AZURE_OPENAI_KEY=your-azure-key
# AZURE_OPENAI_DEPLOYMENT=your-deployment-name

# =============================================================================
# Application Settings
# =============================================================================

# Debug mode (enables detailed logging)
DEBUG=false

# Default data classification
DEFAULT_CLASSIFICATION=INTERNAL

# =============================================================================
# Storage (future use)
# =============================================================================

# DATABASE_URL=sqlite:///./urs_generator.db
