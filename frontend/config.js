// Configuration for URS Generator Frontend
// This file is loaded before app.js

window.APP_CONFIG = {
    // API URL - uses relative path for Vercel deployment
    // For local development, this will be proxied or use localhost
    API_BASE_URL: '/api'
};

